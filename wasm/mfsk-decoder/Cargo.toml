# SPDX-License-Identifier: GPL-3.0-only
[package]
name = "hamsdr-mfsk-decoder"
version = "0.5.3"
edition = "2021"
license = "GPL-3.0-only"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
js-sys = "0.3"
wasm-bindgen = "0.2"
mfsk-core = { version = "0.11", features = ["ft8", "ft4"] }
